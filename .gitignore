# =============================================================================
# 衛星處理系統 - Git 忽略文件配置
# =============================================================================

# 🔐 敏感配置文件
.env                    # 環境變數文件 (包含敏感信息)
*.env                   # 所有環境變數文件
.env.local
.env.production

# 🐍 Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# 🧪 測試相關
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/
venv/
.venv/
baselines/

# 📊 數據文件 (大型數據不提交)
data/outputs/
!data/outputs/.gitkeep           # 但保留目錄結構
data/validation_snapshots/
!data/validation_snapshots/.gitkeep  # 但保留目錄結構
data/tle_data/**                 # TLE數據文件 (每日更新)
!data/tle_data/.gitkeep          # 但保留目錄結構
data/cache/                      # 快取檔案 (HDF5 座標快取, 可重新產生)
!data/cache/.gitkeep             # 但保留目錄結構
data/logs/*.log
*.csv
*.parquet
*.bsp                            # Skyfield 星歷數據 (17MB+, 自動下載)
data/ephemeris/*.bsp             # 明確忽略星歷數據檔案

# 🐳 Docker
.dockerignore

# 💻 IDE 設定
.vscode/
.idea/
*.swp
*.swo
*~

# 📱 macOS
.DS_Store

# 🪟 Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# 🔧 工具配置
.mypy_cache/
.dmypy.json
dmypy.json

# 📝 日誌文件
*.log
logs/
*.log.*

# 🚀 部署相關
docker-compose.override.yml
.env.production.local

# 📦 依賴相關
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 🔄 暫存文件
tmp/
temp/
*.tmp
*.temp
.cache/
data/cache/

# =============================================================================
# 注意事項:
# 1. .env 文件包含敏感配置，絕對不能提交
# 2. .env.example 作為模板，應該提交到版本控制
# 3. 大型數據文件不提交，重新執行程式即可獲得
# 4. data/outputs/ 和 data/validation_snapshots/ 包含 >1GB 單檔
# 5. 保留 .gitkeep 檔案維持目錄結構
# =============================================================================

# Backup files
*.bak
*.bak[0-9]
