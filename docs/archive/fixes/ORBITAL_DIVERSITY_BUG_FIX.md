# Stage 4 軌道面多樣性 Bug 修復報告

## 📋 修復日期
2025-10-22

## 🐛 Bug 描述

### 問題 1: 配置 `enabled: false` 無效
**症狀**: 即使在 `stage4_link_feasibility_config.yaml` 中設置 `orbital_diversity.enabled: false`，程式仍然執行軌道面約束。

**影響範圍**:
- Starlink: 部分影響（自然 Greedy 算法仍能達標）
- OneWeb: 嚴重影響（覆蓋率從 95% 降至 10%）

### 問題 2: 錯誤的兩階段算法
**症狀**: 程式使用「先選代表，再填補」的兩階段算法，破壞時間覆蓋連續性。

**算法流程**（錯誤）:
```
階段 1: 從每個軌道面選擇 1 顆代表衛星
        → 選出 15 顆（Starlink）或 5 顆（OneWeb）
階段 2: 使用 Greedy 填補到每面上限
        → 選出 45 顆（Starlink）或 9 顆（OneWeb）
```

**為何導致失敗**:
- 階段 1 選出的代表衛星在時間上不重疊
- 階段 2 受限於每面上限，無法選擇時間覆蓋最佳的衛星
- 結果：覆蓋率 0-10%，遠低於 95% 目標

## ✅ 修復方案

### 修復 1: 刪除兩階段算法
**檔案**: `src/stages/stage4_link_feasibility/pool_optimizer.py`

**修改內容**:
1. 刪除 `_select_raan_representatives()` 方法（lines 410-514）
2. 保留單階段 Greedy 算法（lines 104-191）

**正確算法** (已實施):
```
單階段 Greedy + 可選約束:
1. 建立時間點覆蓋映射
2. 迭代選擇貢獻度最高的衛星
3. 【可選】檢查軌道面上限，跳過已達上限的面
4. 持續選擇直到達成 95% 覆蓋率
```

### 修復 2: 更新日誌訊息
**檔案**: `src/stages/stage4_link_feasibility/pool_optimizer.py`

**修改內容**:
- Line 90: "兩階段池優化" → "衛星池優化（帶軌道面多樣性約束）"
- Line 96: "衛星池優化" → "衛星池優化（無軌道面約束）"
- Line 234-238: 增強軌道面統計輸出格式

### 修復 3: 更新配置文件
**檔案**: `config/stage4_link_feasibility_config.yaml`

**修改內容**:
1. Starlink: `enabled: false` + 文獻證據註解
2. OneWeb: `enabled: false` + 文獻證據註解

**新增註解**:
```yaml
# NOTE: 文獻研究顯示軌道面多樣性對 RL 訓練並無明確證據支持
# 保留此配置僅作為觀察指標，不強制約束
orbital_diversity:
  enabled: false  # ❌ 禁用約束（文獻無證據支持）
```

## 📊 預期結果

### 修復前（Bug）
```
Starlink:
  - 選擇數量: 98 顆
  - 覆蓋率: 96.3% ✅（自然 Greedy 仍能運作）
  - 軌道面: 9 個（自然分布）

OneWeb:
  - 選擇數量: 9 顆
  - 覆蓋率: 10.0% ❌（兩階段算法失敗）
  - 軌道面: 5 個（強制代表選擇）
```

### 修復後（預期）
```
Starlink:
  - 選擇數量: ~98-102 顆
  - 覆蓋率: ≥95% ✅
  - 軌道面: 9-12 個（自然分布，無約束）
  - 日誌顯示: "衛星池優化（無軌道面約束）"

OneWeb:
  - 選擇數量: 根據覆蓋需求動態決定
  - 覆蓋率: ≥95% ✅
  - 軌道面: 自然分布（無約束）
  - 日誌顯示: "衛星池優化（無軌道面約束）"
```

## 🔬 文獻研究證據

根據 9 篇 LEO 衛星換手 RL 論文的分析：

**✅ 有明確要求的多樣性**:
1. 時間多樣性 - 不同時間點的換手場景
2. 網路條件多樣性 - 動態傳播條件（LOS/Shadowed/Blocked）
3. 用戶負載多樣性 - 不同衛星負載狀態
4. 用戶需求多樣性 - 不同流量類型（VoIP/Video/IoT）
5. 問題規模多樣性 - 不同衛星數量、用戶數量

**❌ 無證據支持的多樣性**:
- 軌道面分佈多樣性 - 無任何論文提及
- RAAN 分佈均勻性 - 無任何論文討論
- 空間幾何多樣性 - 無任何論文要求

**結論**: 軌道面多樣性不是 RL 訓練數據的必要要求，保留作為觀察指標即可。

## 📚 相關文件

- 修改檔案:
  - `src/stages/stage4_link_feasibility/pool_optimizer.py`
  - `config/stage4_link_feasibility_config.yaml`

- 測試日誌:
  - 修復前: `/tmp/stage4_original_test.log`
  - 修復後: `/tmp/stage4_bug_fixed_test.log`

- 論文分析:
  - 來源目錄: `/home/sat/satellite/rl-paper/`
  - 關鍵論文: 9 篇 LEO 衛星換手 RL 論文

## ⚠️ 注意事項

1. **不建議啟用軌道面約束** - 文獻無證據支持，可能降低覆蓋率
2. **保留分析指標** - 仍計算軌道面分佈、Gini 係數等指標
3. **未來改進方向** - 根據文獻建議，應優先實施：
   - Stage 5 擴充: 動態傳播條件（LOS/Shadowed/Blocked）
   - Stage 6 擴充: 多流量類型、衛星負載模擬

## ✅ 驗證清單

- [x] 刪除兩階段算法
- [x] 更新日誌訊息
- [x] 更新配置文件註解
- [ ] 測試修復結果（進行中）
- [ ] 驗證 OneWeb 覆蓋率 ≥95%
- [ ] 驗證軌道面統計正確輸出
- [ ] 文檔更新完成
