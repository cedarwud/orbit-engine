# =============================================================================
# 軌道引擎系統 - 環境變數配置範例
# 複製此文件為 .env 並根據需要修改配置值
# =============================================================================

# 🛰️ 核心系統配置
SATELLITE_ENV=development                    # 環境類型: development, production
VALIDATION_LEVEL=FAST                       # 驗證級別: FAST, STANDARD, STRICT
LOG_LEVEL=DEBUG                             # 日誌級別: DEBUG, INFO, WARNING, ERROR
PYTHONUNBUFFERED=1                          # Python 即時輸出
RELOAD_ON_CHANGE=true                       # 文件變更時重載

# 📂 路徑配置
PYTHONPATH=/orbit-engine/src:/orbit-engine
DATA_BASE_PATH=/orbit-engine/data
TLE_DATA_PATH=/orbit-engine/data/tle_data
OUTPUTS_PATH=/orbit-engine/data/outputs
LOGS_PATH=/orbit-engine/data/logs
VALIDATION_PATH=/orbit-engine/data/validation_snapshots

# 🗄️ 資料庫配置
POSTGRES_HOST=postgres-dev                  # PostgreSQL 主機名
POSTGRES_PORT=5432                          # 容器內端口
POSTGRES_DB=satellite_processing_dev        # 數據庫名稱
POSTGRES_USER=developer                     # 數據庫用戶名
POSTGRES_PASSWORD=dev_password              # 數據庫密碼 (請修改為安全密碼)
DATABASE_URL=postgresql://developer:dev_password@postgres-dev:5432/satellite_processing_dev

# 🔌 網路端口配置 (主機端口)
HEALTH_CHECK_PORT=8900                      # 健康檢查端點
DEV_API_PORT=8901                          # 開發 API 端點
PYTHON_DEBUG_PORT=5678                      # Python 調試端口
POSTGRES_EXTERNAL_PORT=5433                 # PostgreSQL 外部端口

# ⚡ 處理配置
STAGE_TIMEOUT=3600                          # 階段超時時間 (秒)
MAX_CONCURRENT_STAGES=3                     # 最大並發階段數
ENABLE_STAGE_MONITORING=true                # 啟用階段監控

# 🔧 開發工具配置
ENABLE_HOT_RELOAD=true                      # 啟用熱重載
DEBUG_MODE=true                             # 調試模式
PROFILING_ENABLED=false                     # 性能分析 (開發時可啟用)
METRICS_COLLECTION=true                     # 指標收集

# 📊 監控配置
MONITORING_INTERVAL=30                      # 監控間隔 (秒)
HEALTH_CHECK_INTERVAL=30                   # 健康檢查間隔 (秒)
LOG_RETENTION_DAYS=7                       # 日誌保留天數

# 🚨 故障處理
AUTO_RETRY_FAILED_STAGES=true              # 自動重試失敗階段
MAX_RETRY_ATTEMPTS=3                       # 最大重試次數
FAIL_FAST_MODE=false                       # 快速失敗模式

# =============================================================================
# 配置說明:
#
# 1. 複製此文件為 .env:
#    cp .env.example .env
#
# 2. 根據需要修改配置值
#
# 3. 重要提醒:
#    - 請勿將包含敏感信息的 .env 文件提交到版本控制
#    - 生產環境請使用強密碼
#    - 根據實際需求調整端口配置
# =============================================================================