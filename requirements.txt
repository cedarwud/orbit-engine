# 六階段處理系統核心依賴
# orbit-engine 專用（數據生成引擎）
# 更新日期: 2025-10-25

# 🚀 核心計算庫
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# 🛰️ 天體力學與軌道計算
skyfield>=1.45          # 官方 SGP4 Python 實現 (替代已棄用的 PyEphem)
sgp4>=2.20              # Vallado SGP4/SDP4 標準實現
astropy>=7.0.0          # 天文計算標準庫 (CODATA 2018/2022 物理常數 + 座標轉換)
# poliastro>=0.17.0     # 學術級天體動力學庫 (MIT License, NASA 認可, 用於交叉驗證)
                        # ⚠️ 兼容性限制: poliastro 最高支持 Python 3.10
                        # 當前環境: Python 3.12 → 交叉驗證功能已設計為可選（優雅降級）
                        # 如需啟用: 使用 Python 3.8-3.10 環境並取消此行註解

# 📦 HDF5 科學數據格式（階段二/三高效數據儲存）
h5py>=3.9.0

# 📡 ITU-R 無線電傳播模型 (官方實現)
# 用途: Stage 5 大氣衰減計算 (ITU-R P.676-13)
# 來源: https://github.com/inigodelportillo/ITU-Rpy
itur>=0.4.0

# 📊 數據處理
PyYAML>=6.0
pydantic>=2.0.0

# 🌐 HTTP 與 API
requests>=2.31.0
httpx>=0.25.0

# 📁 文件處理
Pillow>=10.0.0

# 🕐 時間處理
python-dateutil>=2.8.0
pytz>=2023.3

# 🛠️ 系統工具
psutil>=5.9.0
tqdm>=4.65.0

# 📊 可選：數據庫支援 (如需要)
psycopg2-binary>=2.9.0
SQLAlchemy>=2.0.0

# 🧪 開發與測試 - TDD/BDD 完整測試套件
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0
pytest-cov>=4.1.0                    # 測試覆蓋率
pytest-benchmark>=4.0.0              # 性能基準測試
pytest-bdd>=7.0.0                    # BDD 場景測試
pytest-html>=4.0.0                   # HTML 測試報告
pytest-json-report>=1.5.0            # JSON 測試報告
pytest-xdist>=3.1.0                  # 並行測試執行
allure-pytest>=2.13.0                # 詳細測試報告

# 📈 日誌與監控
structlog>=23.1.0
rich>=13.5.0

# 🔒 配置管理
python-dotenv>=1.0.0

# 🌐 網路工具 (可選)
aiohttp>=3.8.0

# 📊 數值計算增強
matplotlib>=3.7.0
seaborn>=0.12.0

# 🚀 階段二性能優化套件 (JIT 編譯 + CPU 並行)
# 注意：僅使用 CPU 加速，不使用 GPU

# JIT 編譯器 (CPU 加速)
numba>=0.58.0                       # 支援 CPU JIT 編譯加速

# 並行處理增強
multiprocessing-logging>=0.3.4      # 多進程日誌管理

# 性能分析工具
memory-profiler>=0.60.0             # 記憶體使用分析
line-profiler>=4.0.0                # 代碼性能分析
py-spy>=0.3.14                      # 低開銷性能分析器

# 🎯 Stage 6: 多目標優化
# 更新日期: 2025-10-25
# 說明: 採用學術界標準套件，提升研究可信度

# 多目標優化框架
# SOURCE: Blank & Deb (2020). "pymoo: Multi-Objective Optimization in Python"
#         IEEE Access, vol. 8, pp. 89497-89509
# 用途: Stage 6 衛星換手決策的帕累托最優解搜索
pymoo>=0.6.1

# =============================================================================
# 注意事項:
# 1. ❌ 已移除 RL 訓練相關套件（屬於 handover-rl，不屬於 orbit-engine）
#    - stable-baselines3（RL 訓練）
#    - gymnasium（RL 環境）
#    - torch（深度學習）
#    - tensorboard（訓練監控）
#    - tabulate（評估報告）
#
# 2. ❌ 已移除 GPU 加速套件（orbit-engine 不使用 GPU）
#    - cupy-cuda11x
#    - cupy-cuda12x
#
# 3. ✅ orbit-engine 職責：數據生成（六階段處理）
#    - Stage 1-6: TLE → 軌道計算 → 信號分析 → 換手事件
#    - 生成 HDF5 訓練數據供 handover-rl 使用
#
# 4. ✅ handover-rl 職責：RL 訓練
#    - 使用 orbit-engine 生成的數據
#    - 包含 torch, gymnasium, stable-baselines3 等
# =============================================================================
